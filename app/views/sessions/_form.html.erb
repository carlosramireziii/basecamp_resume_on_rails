<%= render 'alerts' %>
<%= bootstrap_form_with url: [:sessions] do |form| %>
  <%= form.email_field :email %>
  <%= form.password_field :password %>
  <%= form.button 'Log in', class: 'btn btn-success btn-block', data: { disable_with: icon('spinner fa-spin', text: 'Logging in') } %>
<% end %>
